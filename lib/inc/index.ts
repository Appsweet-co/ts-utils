/**
 * Increments a value by one. Same as `val + 1`.
 *
 * Inspried by [Ramda.js](https://ramdajs.com/docs/#inc)
 *
 * ```
 * import { inc } from '@appsweet-co/utils';
 *
 * inc(10);
 * // => 11
 * ```
 */
const inc = (val: number) => val + 1;

export { inc };
