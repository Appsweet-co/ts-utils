/**
 * Decrements a value by one. Same as `val - 1`.
 *
 * Inspried by [Ramda.js](https://ramdajs.com/docs/#dec)
 *
 * ```
 * import { dec } from '@appsweet-co/utils';
 *
 * dec(10);
 * // => 9
 * ```
 */
const dec = (val: number) => val - 1;

export { dec };
