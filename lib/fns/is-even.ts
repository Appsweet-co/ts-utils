
/**
 * Returns true of `x` is an even number.
 *
 * @example
 *
 * ```ts
 * isEven(4);
 * // => true
 *
 * isEven(0);
 * // => true
 *
 * isEven(7);
 * // => false
 * ```
 */
export const isEven = (x: number) => x % 2 === 0;
