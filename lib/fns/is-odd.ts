
/**
 * Returns true of `x` is an odd number.
 *
 * @example
 *
 * ```ts
 * isOdd(13);
 * // => true
 *
 * isOdd(8);
 * // => false
 *
 * isOdd(0);
 * // => false
 * ```
 */
export const isOdd = (x: number) => x % 2 !== 0;
